const wideScreen = 1440;
const desktopWidth = 1024;
const tabletWidth = 768;
const mobileWidth = 450;

export const wideScreenMedia = `@media (max-width: ${wideScreen}px)`;
export const desktopMedia = `@media (max-width: ${desktopWidth}px)`;
export const tabletMedia = `@media (max-width: ${tabletWidth}px)`;
export const mobileMedia = `@media (max-width: ${mobileWidth}px)`;
